/*
  testGemini.mjs - deprecated
  Purpose: This local Node test script is deprecated in favor of the serverless API located at /api/parse.
  When deploying to Vercel, the serverless function will run the GenAI SDK on the server using the
  `GENAI_API_KEY` environment variable (set in Vercel Project Settings).
*/

console.log('testGemini is deprecated. Use /api/parse serverless endpoint instead.');