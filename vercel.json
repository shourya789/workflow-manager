{
  "version": 2,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/$1.ts" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "functions": {
    "api/*.ts": {
      "memory": 256,
      "maxDuration": 60
    }
  }
}